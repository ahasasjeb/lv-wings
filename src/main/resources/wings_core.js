// Legacy NeoForge coremod removed in favour of mixin-based implementation.
// This stub is intentionally left blank so older packaging scripts still succeed if they
// expect the resource to exist. The active behaviour now lives in com.toni.wings.mixin.
